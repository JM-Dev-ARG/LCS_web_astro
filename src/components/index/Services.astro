---
import Bento from "../reutilizables/Bento.astro";
import BentoServices from "../reutilizables/BentoServices.astro";
import Container from "../reutilizables/Container.astro";
import SliderServices from "../reutilizables/SliderServices.astro";
import data from "../../data/cardServices.json";
import CardServices from "../reutilizables/CardServices.astro";
---

<section
  id="services"
  class="relative overflow-hidden scroll-p-[1000px] bg-[#fff5eb] z-10"
>
  <div class="">
    <img
      class="hidden md:block w-auto h-[calc(100%/3)] absolute top-1/2 -left-[20%] xl:-left-[6%] translate-y-[-50%] opacity-70"
      src="/resources/asterisco-fino.svg"
      alt=""
    />
  </div>

  <Container>
    <div class="relative z-10 mt-20 w-full grid place-items-center mx-auto">
      <h1 class="text-[clamp(38px,6vw,100px)] font-extralight z-10">
        Contrata Seguros
      </h1>
      <p
        class="text-[clamp(80px,20vw,200px)] font-bold absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 -z-10 text-lcsPink3 tracking-[-0.1em] opacity-60"
      >
        B B B
      </p>
    </div>
    <div
      class="w-full lg:w-[70%] xl:w-[60%] flex justify-center items-center gap-4"
    >
      <div class="">
        <img
          class="w-[100px] rotate-[225deg]"
          src="/resources/flechas-rojas.svg"
          alt=""
        />
      </div>
      <div>
        <p
          class="text-[clamp(20px,3vw,40px)] font-light text-pretty text-center"
        >
          Si no tenes un <span class="sub relative text-[#518d87]"
            >Broker de seguros</span
          >, te conectamos con uno de confianza
        </p>
      </div>

      <div class="">
        <img
          class="w-[100px] rotate-[45deg]"
          src="/resources/flechas-rojas.svg"
          alt=""
        />
      </div>
    </div>

    <div
      class="hidden w-full md:flex flex-col lg:flex-row gap-4 mx-auto justify-center items-center mt-10"
    >
      {data.map((item) => <CardServices {...item} />)}
    </div>
    <!--  <p class="md:hidden text-[20px] font-light text-center text-pretty">
      Selecciona una cobertura y recibi tu cotización
    </p>
    <div class="md:hidden w-full overflow-x-auto ml-3 sliderMobile">
      <SliderServices />
    </div> -->
  </Container>
</section>

<style>
  .sub::before {
    content: "";
    position: absolute;
    bottom: -50%;
    left: 0%;
    width: 100%;
    height: 40px;
    background-image: url("./resources/surayado.svg");
    background-repeat: no-repeat;
    background-size: contain;
    background-position: center;
  }
  .sliderMobile {
    scrollbar-width: none;
  }
  /*   .card__container {
    position: relative;
    overflow: hidden;
    cursor: pointer;
    flex: 1 0 300px;
    height: 600px;
    border-radius: 20px;

    & .card {
      position: relative;
      display: flex;
      justify-content: center;
      align-items: end;
      color: aliceblue;
      width: 100%;
      height: 100%;
      transition: all 0.5s linear;
      background-position: center;
      background-size: cover;
      &::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-image: linear-gradient(transparent 1%, #1d312f 100%);
        -webkit-mask-image: linear-gradient(transparent 1%, #1d312f 100%);
      }
    }

    & .card__content {
      position: absolute;
      padding: 30px;
      display: flex;
      flex-direction: column;
      justify-content: space-around;
      align-items: center;
      top: 0;
      right: 100%;
      width: 100%;
      height: 100%;
      background-color: #1d312f65;
      opacity: 1;
      transition: all 0.4s linear;
    }

    &:hover .card__content {
      right: 0;
    }

    &:hover .card {
      opacity: 0;
    }
  } */
</style>
